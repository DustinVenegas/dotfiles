# Auto detect text files and perform line-ending normalization
* text=auto

# Files I want normalized
*.sh text eol=lf
*.ps1 text eol=clrf

# All git* files shuld be basic lf
git/git* text eol=lf

# ConEmu prefers lf by default
ConEmu.xml text eol=lf